# Changelog

## Unreleased

- Audit the PFC-facing skills against the official PFC 5.0 HTML help and correct CMAT
  replacement/slot order, OR-combined solve limits, `clean`, particle-generation
  overlap/porosity, clump-volume, FISH callback, wall-motion, energy-tracking and
  geometry-import semantics.
- Correct Burger behavior around `bur_mode`, positive-gap inactivity, missing
  inheritance/method/energy support, and document the PFC5 help's `bur_fric` index-page
  inconsistency without overriding the official verification example/runtime probe.
- Add a digest-bound research-evidence note for a 2025 intersection-rutting thesis,
  while keeping its PFC version, numerical values and research surrogates outside the
  package's production defaults.
- Extend Burger calibration with mixed parallel-bond/Burger contact-population audits,
  tensile-role checks and an explicit accelerated-time invariant/validation contract.
- Extend rutting workflows for vertical-horizontal braking/acceleration loads, vector
  reaction control, free/confined boundary sensitivity, deformation asymmetry and
  tensor-defined shear-depth profiles without conflating them with T 0719 metrics.
- Add a reproducible rutting-evidence CSV contract, plotting script and public demo data
  that preserve solver/equivalent time, signed reactions and surrogate provenance.

## 0.1.0-preview.5 - 2026-08-23

- Add `pfc5-case-handoff` for privacy-safe migration, restore-first auditing,
  interrupted-run recovery, state classification, integrity checks and legacy-state
  quarantine of existing private PFC5 asphalt cases.
- Add a generic read-only handoff auditor and empty private manifest template; neither
  asset contains project paths, parameters, data, source excerpts or saved states.
- Route existing-case work through a cheap integrity/restore audit before a full rebuild,
  while preserving the original acceptance gates and authorization boundaries.

## 0.1.0-preview.4 - 2026-08-19

- Document the upstream `jiangnan030-del/pfc-codex-skills` provenance, preserve its
  MIT notice in independently copyable derived skills, and make the separately
  licensed upstream AGPL child boundary explicit.
- Declare the standards source ledger in Marshall and rutting dependency contracts.
- Reverse-check cross-skill Markdown and inline-code references against
  `dependencies.json`, so a referenced sibling asset cannot be omitted from a trimmed or
  single-skill distribution while validation still passes.

## 0.1.0-preview.3 - 2026-08-19

- Replace the single-source intake header with per-method provenance records that keep
  designation, method, printed-page locator, source digest, reviewer, values and DEM
  mapping attached to each chain node.
- Separate binder-level JTG DSR/BBR/MSCR/LAS evidence from same-material mastic tests and
  mixture-scale calibration/confirmation targets in the Burger route.
- Add a machine-readable standards source ledger with reviewed-copy SHA-256 digests,
  method-page bindings, review status and claim records without bundling standards text.
- Make the governance skill's required-skill manifest portable and validate inline-code
  local paths, including paths that escape the self-contained skills directory.
- Replace keyword-only standards tests with ledger schema, claim, provenance, material-
  level, intake-structure and portable-path contract checks.

## 0.1.0-preview.2 - 2026-08-19

- Add a source-role and method-chain map covering the reviewed JTG asphalt, aggregate,
  design, field-test, maintenance, subgrade and quality-evaluation standards without
  bundling copyrighted standards text.
- Bind current Marshall work to JTG 3410-2025 T 0709-2025 and separate raw curves,
  origin correction, unclear-peak handling and water-sensitivity branches.
- Replace legacy wheel-tracking defaults with the T 0719-2025 pass-count, fixed-position
  measurement and width-coefficient contract.
- Trace mixture gradation and constituent properties to JTG 3432-2024 method families,
  and extend the case intake to preserve the full standard method chain.
- Distinguish binder-level BBR/DSR/MSCR/LAS evidence from mixture-level dynamic-modulus,
  repeated-compression and rutting targets in Burger calibration.

## 0.1.0-preview.1 - 2026-08-19

- Publish 18 PFC 5.0-only asphalt modeling and post-processing skills.
- Add a Codex plugin manifest and release validation.
- Replace the removed knowledge-base CI job with Windows/Linux skill-package checks.
- Separate core, test, and optional visualization dependencies.
- Preserve fistPkg, commercial PFC assets, standards text, binaries, projects, and saves as external dependencies.
