# Examples

- `minimal_case/` contains deterministic CSV inputs for curves, fields, porosity, stress, and orientations.
- `animation_case/` contains unordered PNG frames for frame-order and GIF smoke tests.
- `plugin_migration_case/` contains small legacy text exports for converter tests.

Run `python scripts/run_demo.py` from the skill directory or invoke the script by path.
